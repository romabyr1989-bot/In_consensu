target/

# .mvn/wrapper/maven-wrapper.jar is committed on purpose (NFR-4): without it a clean checkout in a
# closed perimeter cannot start the build at all. Its checksum is pinned in maven-wrapper.properties.

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project
nbproject/

# OS
.DS_Store
Thumbs.db

# Local runtime
*.log
logs/
.env
.env.local

# Фронт: зависимости и промежуточные каталоги сборки Angular. Собранная статика лежит в
# src/main/resources/static/app и попадает в JAR, поэтому она версионируется (ADR-0087).
src/main/frontend/inconsensu-ui/node_modules/
src/main/frontend/inconsensu-ui/.angular/
src/main/frontend/inconsensu-ui/dist/
